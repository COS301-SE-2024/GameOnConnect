import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:gameonconnect/view/pages/messaging/messaging_page.dart';

void main() {
  group('Choose games Widget Tests', () {
    testWidgets('AppBar and button Test', (WidgetTester tester) async {
      /*await tester.pumpWidget(
        const MaterialApp(
          home: Messaging(),
        ),
      );
      await tester.pumpAndSettle();
      //await Future.delayed(const Duration(seconds: 1));
       // Debug prints to check if the widgets are found
      //print('Back button found: ${find.byKey(const Key('Back_button_key')).evaluate().isNotEmpty}');
      //print('Select a game text found: ${find.byKey(const Key('messages_text')).evaluate().isNotEmpty}');
      //await tester.pump();
      //expect(find.byType(BackButtonAppBar), findsOneWidget);
      expect(find.byKey(const Key('Back_button_key'),), findsOneWidget);
      expect(find.byKey(const Key('messages_text'),), findsOneWidget);
      */
    });
  });
}