//import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
//import 'package:gameonconnect/view/components/appbars/backbutton_appbar_component.dart';
//import 'package:gameonconnect/view/pages/messaging/chat_page.dart';

void main() {
  group('Chat Widget Tests', () {
    testWidgets('AppBar and button Test', (WidgetTester tester) async {
      /*await tester.pumpWidget(
        const MaterialApp(
          home: ChatPage(
            //this should probably be replaced with a mock object so that we can test the chat page
                    profileName: 'Test User',
                    receiverID: 'Fake user id',
                    profilePicture: 'Fake Image',
                  ),
        ),
      );
      await tester.pumpAndSettle();

      expect(find.byType(BackButtonAppBar), findsOneWidget);
      expect(find.byKey(const Key('Back_button_key'),), findsOneWidget);
      expect(find.byKey(const Key('select_a_game_text'),), findsOneWidget);*/
    });
  });
}
